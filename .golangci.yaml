linters-settings:
  staticcheck:
    checks: ["all"]

output:
  formats:
    - format: colored-line-number

linters:
  enable:
    - govet
    - staticcheck
    - shadow   # replaces the old govet.check-shadowing
    - gofumpt
    - gosimple
    - errcheck
    - ineffassign